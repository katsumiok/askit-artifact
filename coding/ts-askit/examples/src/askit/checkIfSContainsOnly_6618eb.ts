// Recompilation count: 0
export function checkIfSContainsOnly_6618eb({ s }: {
    s: string;
}): boolean {
    // Check if 's' contains only alphanumeric characters.
    return /^[a-zA-Z0-9]+$/.test(s);
}

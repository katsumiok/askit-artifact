// Recompilation count: 0
export function checkIfNIsWithin_0c71bb({ n, min, max }: {
  n: number;
  min: number;
  max: number;
}): boolean {
  // Check if 'n' is within the range 'min' to 'max'.
  return n >= min && n <= max;
}

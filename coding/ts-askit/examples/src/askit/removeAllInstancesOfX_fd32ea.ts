// Recompilation count: 0
export function removeAllInstancesOfX_fd32ea({ x, xs }: { x: number; xs: number[] }): number[] {
  // Remove all instances of 'x' from 'xs'.
  return xs.filter((item) => item !== x);
}

// Recompilation count: 0
export function calculateTheSumOfAll_397c74({ ns }: {
    ns: number[];
}): number {
    // Calculate the sum of all numbers in 'ns'.
    return ns.reduce((sum, num) => sum + num, 0);
}

// Recompilation count: 0
export function checkIfSContainsThe_0ae0ba({ s, sub }: {
    s: string;
    sub: string;
}): boolean {
    // Check if 's' contains the substring 'sub'.
    return s.includes(sub);
}

// Recompilation count: 0
export function findTheDifferenceBetweenThe_6bcc83({ d1, d2 }: {
    d1: Date;
    d2: Date;
}): number {
    // Find the difference between the dates 'd1' and 'd2'.
    return d2.getTime() - d1.getTime();
}
